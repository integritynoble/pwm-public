# Principle #5 — Structured Illumination Microscopy (SIM)

**Domain:** Microscopy | **Carrier:** Photon | **Difficulty:** Standard (δ=3)
**DAG:** G.structured.stripe --> L.diag.gray --> K.psf.airy --> ∫.temporal | **Reward:** 3× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          G.structured.stripe-->L.diag.gray-->K.psf.airy-->∫.temporal  SIM  BioSR-SIM-15  Reconstruct
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  SIM  P = (E, G, W, C)   Principle #5                          │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ y_k(r) = [I_k(r) · f(r)] ⊛ PSF(r) + n_k             │
│        │ I_k = 1 + m·cos(2π k·r/p + φ_k), k=1..9 (3 angles   │
│        │ × 3 phases); Inverse: recover f at 2× resolution      │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [G.structured.stripe]──→[L.diag.gray]──→[K.psf.airy]──→[∫.temporal]│
│        │  Source(stripe)  Modulate(pattern)  PSF-blur  Accumulate│
│        │ V={G,L,K,∫}  A={G-->L,L-->K,K-->∫}   L_DAG=2.5      │
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (9 eqs for frequency-doubled recovery) │
│        │ Uniqueness: YES (3 angles × 3 phases → full coverage) │
│        │ Stability: κ ≈ 25 (ideal patterns); κ ≈ 100 (errors) │
│        │ Mismatch: pattern freq error, phase drift, OTF atten. │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = PSNR, SSIM, resolution_gain (target: 2×)          │
│        │ q = 2.0 (Wiener-based SIM reconstruction)            │
│        │ T = {residual_norm, fitted_rate, resolution_FRC}      │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | 9 raw frames (3×3) dimensionally consistent with Moiré mixing | PASS |
| S2 | Pattern frequency at OTF edge → κ ≈ 25, well-posed | PASS |
| S3 | Wiener-SIM converges; fairSIM baseline available | PASS |
| S4 | 2× resolution gain, PSNR ≥ 28 dB achievable | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# sim/biosr_s1_ideal.yaml
principle_ref: sha256:<p005_hash>
omega:
  grid: [512, 512]       # output super-resolved
  raw_grid: [256, 256]   # each of 9 raw frames
  pixel_nm: 65
  pattern_period_nm: 204
  NA: 1.49
E:
  forward: "y_k = (I_k · f) ⊛ PSF + n, k=1..9"
  angles: 3
  phases_per_angle: 3
I:
  dataset: BioSR_SIM_15
  images: 15
  noise: {type: poisson, peak: 2000}
  scenario: ideal
O: [PSNR, SSIM, FRC_resolution_nm]
epsilon:
  PSNR_min: 28.0
  SSIM_min: 0.82
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 9 frames at 256² → 512² output: 2× super-resolution consistent | PASS |
| S2 | Pattern at NA=1.49: κ ≈ 25, bounded inverse | PASS |
| S3 | fairSIM converges in closed form; iterative SIM at O(1/k²) | PASS |
| S4 | PSNR ≥ 28 dB at 2000 photons/px | PASS |

**Layer 2 reward:** 105 PWM + upstream

---

## Layer 3 — spec → Benchmark

```yaml
# sim/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec005_hash>
dataset:
  name: BioSR_SIM_15
  images: 15
  raw_size: [256, 256, 9]
  output_size: [512, 512]
baselines:
  - solver: fairSIM
    params: {wiener: 0.002}
    results: {PSNR: 28.6, SSIM: 0.834}
  - solver: OpenSIM
    params: {method: wiener, apodize: true}
    results: {PSNR: 29.8, SSIM: 0.862}
  - solver: ML-SIM
    params: {arch: ResNet, pretrained: true}
    results: {PSNR: 33.4, SSIM: 0.935}
quality_scoring:
  - {min: 34.0, Q: 1.00}
  - {min: 31.0, Q: 0.90}
  - {min: 28.0, Q: 0.80}
  - {min: 26.0, Q: 0.75}
```

**Baseline:** fairSIM — PSNR 28.6 dB | **Layer 3 reward:** 60 PWM

---

## Layer 4 — Benchmark → Solution

| Solver | PSNR (dB) | SSIM | Runtime | Q |
|--------|-----------|------|---------|---|
| fairSIM | 28.6 | 0.834 | 0.5 s | 0.80 |
| OpenSIM | 29.8 | 0.862 | 1.2 s | 0.86 |
| ML-SIM | 33.4 | 0.935 | 0.3 s | 0.98 |
| DFCAN | 32.1 | 0.914 | 0.4 s | 0.94 |

### Reward Calculation

```
R = 100 × 1.0 × 3 × 1.0 × Q = 300 × Q
Best (ML-SIM):  300 × 0.98 = 294 PWM
Floor:          300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p005_hash>",
  "r": {"residual_norm": 0.011, "error_bound": 0.025, "ratio": 0.44},
  "c": {"fitted_rate": 1.96, "theoretical_rate": 2.0, "K": 3},
  "Q": 0.94,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–294 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep sim
pwm-node verify sim/biosr_s1_ideal.yaml
pwm-node mine sim/biosr_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
