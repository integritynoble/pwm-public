# Principle #16 — Image Scanning Microscopy (ISM)

**Domain:** Microscopy | **Carrier:** Photon | **Difficulty:** Standard (δ=3)
**DAG:** S.raster --> K.psf.confocal --> ∫.temporal | **Reward:** 3× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          S.raster-->K.psf.confocal-->∫.temporal  ISM  ISM-Cells-10  Reassign+Deconv
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  ISM  P = (E, G, W, C)   Principle #16                        │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ y(r_s, r_d) = PSF_exc(r_s) · PSF_det(r_s - r_d) ⊛ f │
│        │ r_s = scan pos; r_d = detector element (Airyscan 32ch)│
│        │ Pixel-reassignment: shift each detector pixel by r_d/2│
│        │ Inverse: reassign + deconvolve → √2 resolution gain  │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [S.raster] ──→ [K.psf.confocal] ──→ [∫.temporal]      │
│        │  Scan(point)  PSF-blur(confocal)  Accumulate(Airyscan)  │
│        │ V={S,K,∫}  A={S-->K, K-->∫}   L_DAG=2.5              │
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (multi-element detection is redundant) │
│        │ Uniqueness: YES (reassignment is bijective)           │
│        │ Stability: κ ≈ 15 (32-element Airyscan)              │
│        │ Mismatch: detector misalignment, scan drift           │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = PSNR, SSIM, resolution_gain                       │
│        │ q = 2.0 (multi-image Wiener deconvolution)          │
│        │ T = {residual_norm, fitted_rate, FRC_resolution}      │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | 32 detector elements consistent with Airyscan geometry | PASS |
| S2 | Multi-element redundancy → SNR boost + resolution; κ bounded | PASS |
| S3 | Multi-image Wiener converges in closed form | PASS |
| S4 | √2 resolution gain, PSNR ≥ 30 dB achievable | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# ism/cells_s1_ideal.yaml
principle_ref: sha256:<p016_hash>
omega:
  grid: [256, 256]
  detector_elements: 32
  pixel_nm: 40
  emission_nm: 520
  NA: 1.4
E:
  forward: "y(r_s,r_d) = PSF_exc·PSF_det ⊛ f"
I:
  dataset: ISM_Cells_10
  images: 10
  noise: {type: poisson, peak: 1500}
  scenario: ideal
O: [PSNR, SSIM, resolution_gain]
epsilon:
  PSNR_min: 30.0
  SSIM_min: 0.85
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 32 elements at 40 nm pixel: Nyquist for √2× resolution | PASS |
| S2 | κ ≈ 15 with 32-element redundancy | PASS |
| S3 | Wiener deconvolution converges | PASS |
| S4 | PSNR ≥ 30 dB at 1500 photons | PASS |

**Layer 2 reward:** 105 PWM + upstream

---

## Layer 3 — spec → Benchmark

```yaml
# ism/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec016_hash>
dataset:
  name: ISM_Cells_10
  images: 10
  size: [256, 256, 32]
baselines:
  - solver: Pixel-Reassignment
    params: {shift: half}
    results: {PSNR: 30.5, SSIM: 0.862}
  - solver: ISM-Wiener
    params: {lambda: 0.001}
    results: {PSNR: 32.8, SSIM: 0.908}
  - solver: ISM-DL
    params: {arch: ResNet}
    results: {PSNR: 35.4, SSIM: 0.948}
quality_scoring:
  - {min: 36.0, Q: 1.00}
  - {min: 33.0, Q: 0.90}
  - {min: 30.0, Q: 0.80}
  - {min: 28.0, Q: 0.75}
```

**Baseline:** Pixel-Reassignment — PSNR 30.5 dB | **Layer 3 reward:** 60 PWM

---

## Layer 4 — Benchmark → Solution

| Solver | PSNR (dB) | SSIM | Runtime | Q |
|--------|-----------|------|---------|---|
| Pixel-Reassignment | 30.5 | 0.862 | 0.1 s | 0.80 |
| ISM-Wiener | 32.8 | 0.908 | 0.5 s | 0.90 |
| ISM-DL | 35.4 | 0.948 | 0.3 s | 0.98 |
| MFBD-ISM | 33.9 | 0.925 | 2 s | 0.94 |

### Reward Calculation

```
R = 100 × 1.0 × 3 × 1.0 × Q = 300 × Q
Best (ISM-DL):  300 × 0.98 = 294 PWM
Floor:          300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p016_hash>",
  "r": {"residual_norm": 0.008, "error_bound": 0.02, "ratio": 0.40},
  "c": {"fitted_rate": 1.96, "theoretical_rate": 2.0, "K": 3},
  "Q": 0.94,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–294 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep ism
pwm-node verify ism/cells_s1_ideal.yaml
pwm-node mine ism/cells_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
