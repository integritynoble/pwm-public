# Principle #379 — Baryon Acoustic Oscillation (BAO): Four-Layer Walkthrough

**Principle #379: Baryon Acoustic Oscillation (BAO)**
Domain: Astrophysics & Cosmology | Carrier: gravitational | Difficulty: standard (delta=3) | Reward: 3x base

---

## Four-Layer Pipeline

```
LAYER 1              LAYER 2              LAYER 3              LAYER 4
seeds -> Useful(B)   Principle + S1-S4    spec.md + Principle   spec.md + Benchmark
designs the          designs              + S1-S4 designs &     + Principle + S1-S4
PRINCIPLE            spec.md (PoInf)      verifies BENCHMARK    verifies SOLUTION

[Seed] --> [Principle] --> [spec.md] --> [Benchmark] --> [Solution]
  L1          L1              L2             L3             L4
```

---

## Layer 1: Seeds -> Principle

### Governing Equation

```
ξ(s) = ∫ P(k) j₀(ks) k² dk / (2π²)    (correlation function from power spectrum)
P(k) = P_smooth(k) × [1 + A sin(k r_s)/k r_s]  (BAO wiggle feature)
r_s = ∫₀^{z_drag} c_s(z)/H(z) dz        (sound horizon at drag epoch)
D_V(z) = [D_A²(z) c z / H(z)]^{1/3}     (volume-averaged distance)
```

### DAG Decomposition G = (V, A)

```
[F.dft] -> [∫.volume] -> [O.l2]

V = {F.dft, ∫.volume, O.l2}
A = {F.dft->∫.volume, ∫.volume->O.l2}
L_DAG = 2.0   Tier: standard (delta = 3)
```

### Well-Posedness Certificate

| Property | Status |
|----------|--------|
| Existence | YES -- acoustic scale is a physical feature in matter distribution |
| Uniqueness | YES -- r_s uniquely set by pre-recombination physics |
| Stability | YES -- BAO peak position robust against nonlinear evolution |

Mismatch parameters: nonlinear damping, redshift-space distortions, survey window, photo-z scatter

### Error-Bounding Method

```
e  = D_V/r_s relative error (primary), BAO peak position error (secondary)
q = 0.5 (statistical scaling with survey volume)
T  = {residual_norm, error_bound, convergence_rate, fitted_rate, K_resolutions, quality_q}
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Distance [Mpc], wavenumber [h/Mpc], correlation function dimensionless | PASS |
| S2 | BAO peak position well-determined for V_eff > 1 Gpc³ | PASS |
| S3 | Template fitting converges; reconstruction improves S/N | PASS |
| S4 | D_V/r_s measurable to 1-2% per redshift bin | PASS |

### Layer 1 Reward

```
Principle seed reward = 200 x phi(t) PWM
Upstream: 15% of L2 seeds + 10% of L3 seeds + 5% of L4 mints + 5% of L4 usage
```

---

## Layer 2: Principle -> spec.md

### Example spec.md (S1 Ideal Scenario)

```yaml
principle_ref: sha256:<principle_379_hash>

omega:
  description: "BAO measurement from galaxy survey, z=0.5, V=2 Gpc³"
  redshift: 0.5
  volume_Gpc3: 2.0
  outputs: [DV_rs, alpha_perp, alpha_para]

E:
  forward: "ξ(s) = ∫ P(k)j₀(ks)k²dk/(2π²)"
  dag: "[F.dft] -> [∫.volume] -> [O.l2]"

B:
  constraints: "r_s > 0; isotropic or anisotropic fit; window function applied"

I:
  scenario: ideal
  cosmology: {Omega_m: 0.3, h: 0.7, r_s: 147.5}
  mismatch: null

O: [DV_rs_error, peak_position_error]

epsilon:
  DV_err_max: 0.02
  convergence_order: 0.5

difficulty:
  L_DAG: 3.0
  tier: standard
  delta: 3
```

### S1-S4 Scenarios

| Scenario | Operator | Mismatch | Threshold |
|----------|----------|----------|-----------|
| S1 Ideal | Known cosmology + complete survey | None | DV_err < 2% |
| S2 Mismatch | Nonlinear damping or window effect | Applied | relaxed 1.5x |
| S3 Oracle | True distances known | Known | DV_err < 2% |
| S4 Blind Cal | Measure BAO from galaxy catalog | Unknown | recover >= 85% of oracle gap |

### Layer 2 Reward

```
spec.md seed reward = 150 x phi(t) x 0.70 = 105 PWM (designer)
Upstream: 15% -> L1 creator, 15% -> treasury
```

---

## Layer 3: spec.md -> Benchmark

### Benchmark Configuration

```yaml
spec_ref: sha256:<spec_379_hash>
principle_ref: sha256:<principle_379_hash>

dataset:
  description: "Mock galaxy catalogs with BAO feature at multiple redshifts"
  redshifts: [0.3, 0.5, 0.7, 1.0]
  data_hash: sha256:<dataset_379_hash>

baselines:
  - solver: Reconstruction+Template  DV_error: 0.010    q: 0.95
  - solver: Template-only            DV_error: 0.015    q: 0.90
  - solver: Direct-ξ peak            DV_error: 0.025    q: 0.80

quality_scoring:
  metric: DV_rs_error
  thresholds:
    - {max: 0.005, Q: 1.00}
    - {max: 0.010, Q: 0.90}
    - {max: 0.020, Q: 0.80}
    - {max: 0.030, Q: 0.75}
```

### Baselines

| Solver | DV_error | Q | Approx Reward |
|--------|---------|---|---------------|
| Reconstruction+Template | 0.010 | 0.95 | ~285 PWM |
| Template-only | 0.015 | 0.90 | ~270 PWM |
| Direct-ξ peak | 0.025 | 0.80 | ~240 PWM |

### S1-S4 Gate Checks (Layer 3)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Redshifts and volumes match spec; mock catalog format consistent | PASS |
| S2 | Problem well-posed: mock catalogs have known input cosmology | PASS |
| S3 | Reconstruction + template converges; peak sharpens with reconstruction | PASS |
| S4 | Baseline meets threshold (DV_err < 2%); feasibility confirmed | PASS |

### Layer 3 Reward

```
Benchmark seed reward = 100 x phi(t) x 0.60 = 60 PWM (builder)
Upstream: 15% -> L2 designer, 10% -> L1 creator, 15% -> treasury
```

---

## Layer 4: Benchmark -> Solution (PoSol Mining)

### Solver Table

| Solver | Expected Error | Time | Q | Reward |
|--------|---------------|------|---|--------|
| Reconstruction+Template | 1.0% | ~10 min | 0.95 | ~285 PWM |
| Template-only | 1.5% | ~2 min | 0.90 | ~270 PWM |
| Direct-ξ peak | 2.5% | ~1 min | 0.80 | ~240 PWM |

### Reward Calculation

```
R = R_base x phi(t) x delta x nu_c x q
  = 100 x 1.0 x 3 x 1.0 x q
  = 300 x q  PWM

Best case:  300 x 0.95 = 285 PWM
Worst case: 300 x 0.75 = 225 PWM
```

### S4 Certificate

```json
{
  "principle": "#379 Baryon Acoustic Oscillation (BAO)",
  "h_p": "sha256:<principle_379_hash>",
  "h_s": "sha256:<spec_379_hash>",
  "h_b": "sha256:<bench_379_hash>",
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"},
  "Q": 0.95,
  "difficulty": {"tier":"standard","delta":3}
}
```

---

## Reward Summary

```
L1 Principle:  200 PWM seed + upstream royalties from L2/L3/L4
L2 spec.md:    105 PWM seed (x4 scenarios = 420 PWM) + upstream from L3/L4
L3 Benchmark:   60 PWM seed (x4 benchmarks = 240 PWM) + upstream from L4
L4 Solution:   225-285 PWM per solution (depending on q)
```

---

## Quick-Start

```bash
pwm-node benchmarks | grep bao
pwm-node verify AC_astrophysics/bao_s1_ideal.yaml
pwm-node mine AC_astrophysics/bao_s1_ideal.yaml
pwm-node inspect sha256:<your_cert_hash>
```
