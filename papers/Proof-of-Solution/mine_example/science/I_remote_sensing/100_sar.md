# Principle #100 — Synthetic Aperture Radar (SAR)

**Domain:** Remote Sensing | **Carrier:** Microwave | **Difficulty:** Hard (δ=5)
**DAG:** G.pulse --> K.green --> F.dft --> integral.aperture | **Reward:** 5× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │        G.pulse-->K.green-->F.dft-->integral.aperture    SAR-img    SAR-SpaceNet        Focusing
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  SAR   P = (E, G, W, C)   Principle #100                       │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ y(τ,η) = ∫∫ σ(x,y)·exp(−j4πf_c R(x,y;η)/c)          │
│        │           · rect((τ−2R/c)/T_p) dx dy + n              │
│        │ Coherent pulse: matched filter in range + azimuth      │
│        │ Inverse: recover reflectivity σ(x,y) from raw echoes   │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [G.pulse] --> [K.green] --> [F.dft] --> [integral.aperture]│
│        │ Chirp  Propagate  RangeCompress  AzimuthSynth           │
│        │ V={G.pulse, K.green, F.dft, integral.aperture}  A={G.pulse-->K.green, K.green-->F.dft, F.dft-->integral.aperture}   L_DAG=4.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (matched filter is well-defined)        │
│        │ Uniqueness: YES for broadside geometry                  │
│        │ Stability: κ ≈ 10 (stripmap), κ ≈ 40 (spotlight)      │
│        │ Mismatch: motion errors, atmospheric phase screen       │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = PSNR (primary), ISLR/PSLR (secondary)              │
│        │ q = 2.0 (matched filter + autofocus convergence)      │
│        │ T = {residual_norm, focus_quality, K_resolutions}       │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Pulse bandwidth and synthetic aperture consistent with resolution | PASS |
| S2 | Matched filter well-conditioned; autofocus regularizable | PASS |
| S3 | Range-Doppler / omega-k algorithm converges | PASS |
| S4 | PSNR ≥ 25 dB achievable for focused SAR imagery | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# sar/spacenet_s1_ideal.yaml
principle_ref: sha256:<p100_hash>
omega:
  grid: [2048, 2048]
  pixel_m: 1.0
  frequency_GHz: 9.65
  bandwidth_MHz: 150
  mode: stripmap
E:
  forward: "y = MF_range(MF_azimuth(σ)) + n"
I:
  dataset: SAR_SpaceNet
  scenes: 50
  noise: {type: speckle, looks: 4}
  scenario: ideal
O: [PSNR, SSIM]
epsilon:
  PSNR_min: 25.0
  SSIM_min: 0.75
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 150 MHz bandwidth → 1 m range resolution; stripmap geometry valid | PASS |
| S2 | 4-look averaging reduces speckle; κ ≈ 10 | PASS |
| S3 | Range-Doppler focusing converges for stripmap | PASS |
| S4 | PSNR ≥ 25 dB after despeckle at 4 looks | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# sar/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec100_hash>
principle_ref: sha256:<p100_hash>
dataset:
  name: SAR_SpaceNet
  scenes: 50
  size: [2048, 2048]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: Lee-Filter
    params: {window: 7}
    results: {PSNR: 26.1, SSIM: 0.782}
  - solver: SAR-BM3D
    params: {sigma_est: auto}
    results: {PSNR: 28.5, SSIM: 0.841}
  - solver: SAR-DnCNN
    params: {pretrained: true}
    results: {PSNR: 31.2, SSIM: 0.905}
quality_scoring:
  - {min: 32.0, Q: 1.00}
  - {min: 29.0, Q: 0.90}
  - {min: 26.0, Q: 0.80}
  - {min: 24.0, Q: 0.75}
```

**Baseline solver:** Lee-Filter — PSNR 26.1 dB
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | PSNR (dB) | SSIM | Runtime | Q |
|--------|-----------|------|---------|---|
| Lee-Filter | 26.1 | 0.782 | 0.5 s | 0.80 |
| SAR-BM3D | 28.5 | 0.841 | 5 s | 0.88 |
| SAR-DnCNN | 31.2 | 0.905 | 1 s | 0.95 |
| SAR-Transformer | 32.5 | 0.932 | 3 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 5 × 1.0 × Q
Best case:  500 × 1.00 = 500 PWM
Floor:      500 × 0.75 = 375 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p100_hash>",
  "h_s": "sha256:<spec100_hash>",
  "h_b": "sha256:<bench100_hash>",
  "r": {"residual_norm": 0.012, "error_bound": 0.03, "ratio": 0.40},
  "c": {"fitted_rate": 1.93, "theoretical_rate": 2.0, "K": 3},
  "Q": 0.95,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 375–500 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep sar
pwm-node verify sar/spacenet_s1_ideal.yaml
pwm-node mine sar/spacenet_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
