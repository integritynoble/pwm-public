# Principle #104 — Ground-Penetrating Radar (GPR)

**Domain:** Remote Sensing | **Carrier:** Microwave (UWB) | **Difficulty:** Standard (δ=3)
**DAG:** G.pulse --> K.green.em --> integral.temporal | **Reward:** 3× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          G.pulse-->K.green.em-->integral.temporal    GPR-img    GPR-Subsurface      Migration
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  GPR   P = (E, G, W, C)   Principle #104                       │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ y(x,t) = Σ_k r_k·s(t − 2d_k(x)/v(z)) + n(x,t)      │
│        │ EM pulse propagates into ground; reflects at ε changes │
│        │ v(z) = c/√ε_r(z); hyperbolic diffraction patterns     │
│        │ Inverse: migrate B-scans to recover subsurface profile │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [G.pulse] --> [K.green.em] --> [integral.temporal]       │
│        │ EMPulse  SubsurfaceProp  A-Scan                         │
│        │ V={G.pulse, K.green.em, integral.temporal}  A={G.pulse-->K.green.em, K.green.em-->integral.temporal}   L_DAG=2.5│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (migration is Radon-type inverse)       │
│        │ Uniqueness: YES for piecewise-constant ε(z)            │
│        │ Stability: κ ≈ 15 (shallow), κ ≈ 80 (deep/lossy)      │
│        │ Mismatch: velocity model error, antenna coupling        │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = PSNR (primary), detection rate (secondary)         │
│        │ q = 2.0 (Kirchhoff migration convergence)             │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Pulse bandwidth consistent with depth resolution target | PASS |
| S2 | Migration operator well-conditioned with known velocity | PASS |
| S3 | Kirchhoff / FK migration converge | PASS |
| S4 | PSNR ≥ 22 dB achievable for shallow targets (<3 m) | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# gpr/subsurface_s1_ideal.yaml
principle_ref: sha256:<p104_hash>
omega:
  scan_length_m: 20
  traces: 512
  samples_per_trace: 1024
  center_freq_MHz: 400
E:
  forward: "y = Σ r_k·s(t − 2d/v) + n"
  velocity_model: "constant ε_r = 6"
I:
  dataset: GPR_Subsurface
  profiles: 40
  noise: {type: gaussian, SNR_dB: 25}
  scenario: ideal
O: [PSNR, detection_rate_pct]
epsilon:
  PSNR_min: 22.0
  detection_rate_min_pct: 90.0
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 400 MHz center freq → ~15 cm resolution in ε_r=6 soil | PASS |
| S2 | κ ≈ 15 with known velocity model | PASS |
| S3 | Kirchhoff migration converges for 512 traces | PASS |
| S4 | PSNR ≥ 22 dB and detection > 90% feasible | PASS |

**Layer 2 reward:** 105 PWM + upstream

---

## Layer 3 — spec → Benchmark

```yaml
# gpr/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec104_hash>
principle_ref: sha256:<p104_hash>
dataset:
  name: GPR_Subsurface
  profiles: 40
  size: [512, 1024]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: Kirchhoff-Migration
    results: {PSNR: 23.5, detection_pct: 92.1}
  - solver: FK-Migration
    results: {PSNR: 25.2, detection_pct: 94.8}
  - solver: GPRNet
    results: {PSNR: 28.8, detection_pct: 97.5}
quality_scoring:
  - {min: 29.0, Q: 1.00}
  - {min: 26.0, Q: 0.90}
  - {min: 23.0, Q: 0.80}
  - {min: 21.0, Q: 0.75}
```

**Baseline:** Kirchhoff — PSNR 23.5 dB | **Layer 3 reward:** 60 PWM + upstream

---

## Layer 4 — Benchmark → Solution

| Solver | PSNR (dB) | Detection (%) | Q |
|--------|-----------|---------------|---|
| Kirchhoff | 23.5 | 92.1 | 0.80 |
| FK-Migration | 25.2 | 94.8 | 0.88 |
| GPRNet | 28.8 | 97.5 | 0.98 |
| SubsurfaceFormer | 29.5 | 98.2 | 1.00 |

### Reward: `R = 100 × 3 × q` → Best: 300 PWM | Floor: 225 PWM

```json
{
  "h_p": "sha256:<p104_hash>",
  "Q": 0.98,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

## Quick-Start

```bash
pwm-node benchmarks | grep gpr
pwm-node mine gpr/subsurface_s1_ideal.yaml
```
