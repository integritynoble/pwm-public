# Principle #151 — Compressed Ultrafast Photography (CUP)

**Domain:** Ultrafast Imaging | **Carrier:** Photon | **Difficulty:** Frontier (δ=8)
**DAG:** L.shear.temporal --> L.diag.binary --> integral.temporal | **Reward:** 8× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          L.shear.temporal-->L.diag.binary-->integral.temporal    CUP         CUPSim-10          TwIST/CS
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  COMPRESSED ULTRAFAST PHOTOGRAPHY   P = (E, G, W, C)   #151    │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ y = Φ · x + n                                         │
│        │ Φ = CS encoding (DMD mask) + temporal shearing (streak)│
│        │ x = 3D scene cube (x, y, t); y = 2D encoded image     │
│        │ Inverse: recover x from single-shot y via CS/TV recon  │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [L.shear.temporal] --> [L.diag.binary] --> [integral.temporal]│
│        │  TemporalShear  CodedMask  Integrate                   │
│        │ V={L.shear.temporal, L.diag.binary, integral.temporal}  A={L.shear.temporal-->L.diag.binary, L.diag.binary-->integral.temporal}   L_DAG=1.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (RIP holds for random binary masks)     │
│        │ Uniqueness: YES (CS theory: m ≥ O(s log N))           │
│        │ Stability: κ ≈ 20 (sparse scenes), κ ≈ 100 (dense)    │
│        │ Mismatch: mask calibration, streak nonlinearity, depth │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = PSNR (primary), SSIM_3D (secondary)               │
│        │ q = 1.0 (TwIST/ADMM sub-linear O(1/k) convergence)  │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Mask pixel count, streak speed, and frame rate yield consistent (x,y,t) sampling | PASS |
| S2 | Sparsity s ≤ 0.1N with RIP-satisfying mask guarantees unique CS recovery | PASS |
| S3 | TwIST converges within 200 iterations for TV-regularized CS | PASS |
| S4 | PSNR ≥ 22 dB achievable for 10% sparsity scenes at 10 Tfps | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# cup/cupsim_s1.yaml
principle_ref: sha256:<p151_hash>
omega:
  spatial_grid: [256, 256]
  temporal_frames: 50
  pixel_um: 10
  frame_rate_Tfps: 10
  mask: random_binary_50pct
E:
  forward: "y = Phi * x + n (CS encoding + streak shearing)"
  reconstruction: "TwIST_TV"
I:
  dataset: CUPSim_10
  scenes: 10
  noise: {type: gaussian, sigma: 0.05}
  scenario: ideal
O: [PSNR, SSIM_3D]
epsilon:
  PSNR_min: 20.0
  SSIM_3D_min: 0.75
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 256×256×50 cube with random binary mask at 50% fill rate | PASS |
| S2 | κ ≈ 20 for sparse ultrafast events with RIP mask | PASS |
| S3 | TwIST converges within 150 iterations at σ = 0.05 | PASS |
| S4 | PSNR ≥ 20 dB feasible for sparse transient scenes | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# cup/benchmark_s1.yaml
spec_ref: sha256:<spec151_hash>
principle_ref: sha256:<p151_hash>
dataset:
  name: CUPSim_10
  scenes: 10
  cube_size: [256, 256, 50]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: TwIST-TV
    params: {n_iter: 200, lambda: 0.01}
    results: {PSNR: 22.5, SSIM_3D: 0.78}
  - solver: GAP-TV
    params: {n_iter: 150, lambda: 0.005}
    results: {PSNR: 24.0, SSIM_3D: 0.83}
  - solver: PnP-FFDNet
    params: {n_iter: 50, denoiser: FFDNet}
    results: {PSNR: 27.5, SSIM_3D: 0.90}
quality_scoring:
  - {min: 27.0, Q: 1.00}
  - {min: 24.0, Q: 0.90}
  - {min: 21.0, Q: 0.80}
  - {min: 18.0, Q: 0.75}
```

**Baseline solver:** GAP-TV — PSNR 24.0 dB
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | PSNR (dB) | SSIM_3D | Runtime | Q |
|--------|-----------|---------|---------|---|
| TwIST-TV | 22.5 | 0.78 | 5 min | 0.80 |
| GAP-TV | 24.0 | 0.83 | 3 min | 0.90 |
| PnP-FFDNet | 27.5 | 0.90 | 1 min | 1.00 |
| E2E-CUP (UNet3D) | 28.2 | 0.92 | 10 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 8 × 1.0 × Q
Best case (PnP/E2E):   800 × 1.00 = 800 PWM
Floor:                  800 × 0.75 = 600 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p151_hash>",
  "h_s": "sha256:<spec151_hash>",
  "h_b": "sha256:<bench151_hash>",
  "r": {"residual_norm": 0.04, "error_bound": 0.10, "ratio": 0.40},
  "c": {"fitted_rate": 0.95, "theoretical_rate": 1.0, "K": 4},
  "Q": 1.00,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 600–800 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep cup
pwm-node verify cup/cupsim_s1.yaml
pwm-node mine cup/cupsim_s1.yaml
pwm-node inspect sha256:<cert_hash>
```
