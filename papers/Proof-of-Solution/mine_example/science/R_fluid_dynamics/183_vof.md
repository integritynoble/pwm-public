# Principle #183 — Volume of Fluid (VOF) Method

**Domain:** Fluid Dynamics | **Carrier:** N/A (PDE-based) | **Difficulty:** Standard (δ=3)
**DAG:** [∂.time] --> [N.bilinear.advection] --> [L.poisson] --> [∫.volume] |  **Reward:** 3× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          ∂.time→N.bilinear.advection→L.poisson→∫.volume   VOF         bubble-rise-2D    FVM
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  VOLUME OF FLUID (VOF)   P = (E,G,W,C)   Principle #183        │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ ∂α/∂t + ∇·(αu) = 0    (VOF advection, α ∈ [0,1])    │
│        │ ρ = αρ₁ + (1−α)ρ₂;  μ = αμ₁ + (1−α)μ₂               │
│        │ ∂(ρu)/∂t + ∇·(ρu⊗u) = −∇p + ∇·τ + ρg + F_σ         │
│        │ F_σ = σκn̂δ_s (surface tension via CSF)                │
│        │ Forward: IC/BC/fluid properties → track interface     │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [∂.time] --> [N.bilinear.advection] --> [L.poisson] --> [∫.volume]│
│        │ time  NS-advection  pressure-solve  VOF-volume-track              │
│        │ V={∂.time,N.bilinear.advection,L.poisson,∫.volume}  L_DAG=3.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (weak sol. for two-phase NS)            │
│        │ Uniqueness: YES for smooth interfaces                  │
│        │ Stability: CFL on both phases; surface tension CFL    │
│        │ Mismatch: surface tension σ, density ratio, spurious  │
│        │   currents from CSF                                    │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = interface position error, mass conservation error  │
│        │ q = 1.0 (geometric VOF), 2.0 (PLIC)                  │
│        │ T = {interface_err, mass_loss, spurious_vel_magnitude} │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | VOF advection + NS coupled consistently; α bounded [0,1] | PASS |
| S2 | PLIC reconstruction maintains sharp interface; mass conserved | PASS |
| S3 | Geometric VOF + projection method converges; mass < 10⁻¹⁰ | PASS |
| S4 | Interface position error bounded by mesh size | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# vof/bubble_rise_s1.yaml
principle_ref: sha256:<p183_hash>
omega:
  grid: [128, 256]
  domain: [1, 2]
  time: [0, 3.0]
E:
  forward: "NS + VOF with PLIC reconstruction"
  surface_tension: 24.5
  gravity: [0, -0.98]
B:
  walls: {no_slip: true}
  top_bottom: {free_slip: true}
I:
  scenario: rising_bubble_2D
  rho: [1000, 100]
  mu: [10, 1]
  bubble_radius: 0.25
  mesh_sizes: [64x128, 128x256]
O: [centroid_position_error, rise_velocity_error, mass_conservation]
epsilon:
  position_error_max: 2.0e-2
  mass_error_max: 1.0e-6
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | Grid resolves bubble; density ratio 10:1 manageable | PASS |
| S2 | Hysing et al. (2009) benchmark solution exists | PASS |
| S3 | PLIC-VOF + SIMPLE converges; mass conserved to 10⁻⁶ | PASS |
| S4 | Centroid position < 2% of bubble diameter | PASS |

**Layer 2 reward:** 105 PWM

---

## Layer 3 — spec → Benchmark

```yaml
# vof/benchmark_bubble.yaml
spec_ref: sha256:<spec183_hash>
principle_ref: sha256:<p183_hash>
dataset:
  name: Hysing_bubble_benchmark
  reference: "Hysing et al. (2009) quantitative benchmark"
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: OpenFOAM-interFoam (MULES)
    params: {grid: 128x256}
    results: {pos_error: 1.5e-2, mass_loss: 2.1e-4}
  - solver: Basilisk (geometric VOF)
    params: {grid: 128x256}
    results: {pos_error: 8.2e-3, mass_loss: 1.5e-8}
  - solver: PARIS (PLIC)
    params: {grid: 128x256}
    results: {pos_error: 6.5e-3, mass_loss: 2.3e-9}
quality_scoring:
  - {min_pos_err: 5.0e-3, Q: 1.00}
  - {min_pos_err: 1.0e-2, Q: 0.90}
  - {min_pos_err: 2.0e-2, Q: 0.80}
  - {min_pos_err: 4.0e-2, Q: 0.75}
```

**Baseline solver:** Basilisk — position error 8.2×10⁻³
**Layer 3 reward:** 60 PWM

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | Position Err | Mass Loss | Runtime | Q |
|--------|-------------|-----------|---------|---|
| interFoam (MULES) | 1.5e-2 | 2.1e-4 | 60 s | 0.80 |
| Basilisk (geom VOF) | 8.2e-3 | 1.5e-8 | 45 s | 0.90 |
| PARIS (PLIC) | 6.5e-3 | 2.3e-9 | 50 s | 0.90 |
| Gerris (adaptive) | 4.1e-3 | 5.8e-10 | 30 s | 1.00 |

### Reward Calculation

```
R = 100 × 1.0 × 3 × 1.0 × Q
Best case (Gerris): 300 × 1.00 = 300 PWM
Floor:              300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p183_hash>",
  "h_s": "sha256:<spec183_hash>",
  "h_b": "sha256:<bench183_hash>",
  "r": {"residual_norm": 4.1e-3, "error_bound": 2.0e-2, "ratio": 0.205},
  "c": {"fitted_rate": 1.92, "theoretical_rate": 2.0, "K": 2},
  "Q": 1.00,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep vof
pwm-node verify vof/bubble_rise_s1.yaml
pwm-node mine vof/bubble_rise_s1.yaml
pwm-node inspect sha256:<cert_hash>
```
