# Principle #179 — Large Eddy Simulation (LES)

**Domain:** Fluid Dynamics | **Carrier:** N/A (PDE-based) | **Difficulty:** Frontier (δ=10)
**DAG:** [∂.time] --> [N.bilinear.advection] --> [K.filter] --> [L.poisson] |  **Reward:** 10× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          ∂.time→N.bilinear.advection→K.filter→L.poisson LES-SGS     channel-Re180     FVM/spectral
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  LARGE EDDY SIMULATION   P = (E,G,W,C)   Principle #179         │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ ∂ū/∂t + ∇·(ū⊗ū) = −∇p̄/ρ + ν∇²ū − ∇·τˢᵍˢ + f       │
│        │ ∇·ū = 0                                                │
│        │ τˢᵍˢ = −2νₛᵍₛS̄ᵢⱼ  (Smagorinsky or dynamic model)    │
│        │ Resolves large eddies; models subgrid-scale (SGS)      │
│        │ Forward: IC/BC/SGS model → time-resolved (ū,p̄)        │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [∂.time] --> [N.bilinear.advection] --> [K.filter] --> [L.poisson]│
│        │ time  advection  SGS-filter  pressure-solve                       │
│        │ V={∂.time,N.bilinear.advection,K.filter,L.poisson}  L_DAG=5.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (filtered NS well-posed with SGS model)│
│        │ Uniqueness: statistical — mean and 2nd-order stats     │
│        │ Stability: CFL < 1 required; explicit time-stepping   │
│        │ Mismatch: SGS model constant Cs, filter width choice  │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = mean velocity profile error, Reynolds stress error │
│        │ q = 2.0 (2nd-order FVM), higher for spectral LES     │
│        │ T = {mean_vel_err, uv_stress_err, energy_spectrum}    │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Filtered NS + SGS closure dimensionally consistent | PASS |
| S2 | Filtered equations well-posed with positive νₛᵍₛ | PASS |
| S3 | Time-stepping converges; statistics converge with averaging time | PASS |
| S4 | Mean velocity < 5% error vs DNS; Reynolds stress < 15% error | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# les/channel_Re180_s1.yaml
principle_ref: sha256:<p179_hash>
omega:
  grid: [64, 64, 64]
  domain: [4π, 2, 4π/3]   # Lx, Ly, Lz
  time: [0, 200]           # flow-through times
E:
  forward: "filtered NS + dynamic Smagorinsky SGS"
  SGS: dynamic_smagorinsky
B:
  walls: {no_slip: true, y_plus_1: true}
  streamwise: periodic
  spanwise: periodic
  driving: {pressure_gradient: constant}
I:
  scenario: channel_LES
  Re_tau: 180
  mesh_sizes: [32x32x32, 64x64x64]
O: [mean_velocity_error, Reynolds_stress_error, energy_spectrum_slope]
epsilon:
  mean_vel_error_max: 5.0e-2
  uv_error_max: 1.5e-1
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | Domain 4π×2×4π/3 sufficient for two-point correlations | PASS |
| S2 | Re_τ=180 manageable for LES; y⁺=1 resolves viscous sublayer | PASS |
| S3 | Dynamic Smag. converges; 200 FTT adequate for statistics | PASS |
| S4 | Mean velocity error < 5% vs KMM DNS data | PASS |

**Layer 2 reward:** 105 PWM

---

## Layer 3 — spec → Benchmark

```yaml
# les/benchmark_channel180.yaml
spec_ref: sha256:<spec179_hash>
principle_ref: sha256:<p179_hash>
dataset:
  name: KMM_DNS_Re180
  reference: "Kim, Moin & Moser (1987) channel DNS"
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: OpenFOAM-pisoFoam (Smagorinsky)
    params: {grid: 64^3, Cs: 0.1}
    results: {mean_vel_err: 4.5e-2, uv_err: 12%}
  - solver: OpenFOAM-pisoFoam (dynamic Smag.)
    params: {grid: 64^3}
    results: {mean_vel_err: 2.8e-2, uv_err: 8%}
  - solver: Spectral LES (WALE)
    params: {grid: 64^3}
    results: {mean_vel_err: 2.2e-2, uv_err: 6%}
quality_scoring:
  - {min_mean_err: 1.0e-2, Q: 1.00}
  - {min_mean_err: 3.0e-2, Q: 0.90}
  - {min_mean_err: 5.0e-2, Q: 0.80}
  - {min_mean_err: 8.0e-2, Q: 0.75}
```

**Baseline solver:** Dynamic Smagorinsky — mean vel error 2.8×10⁻²
**Layer 3 reward:** 60 PWM

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | Mean Vel Err | uv Error | Runtime | Q |
|--------|-------------|----------|---------|---|
| Smagorinsky (Cs=0.1) | 4.5e-2 | 12% | 4 h | 0.80 |
| Dynamic Smagorinsky | 2.8e-2 | 8% | 6 h | 0.90 |
| WALE | 2.2e-2 | 6% | 5 h | 0.90 |
| Vreman | 2.0e-2 | 5.5% | 5 h | 0.90 |

### Reward Calculation

```
R = 100 × 1.0 × 10 × 1.0 × Q
Best case: 1000 × 0.90 = 900 PWM
Floor:     1000 × 0.75 = 750 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p179_hash>",
  "h_s": "sha256:<spec179_hash>",
  "h_b": "sha256:<bench179_hash>",
  "r": {"residual_norm": 2.0e-2, "error_bound": 5.0e-2, "ratio": 0.40},
  "c": {"fitted_rate": 1.85, "theoretical_rate": 2.0, "K": 2},
  "Q": 0.90,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 750–900 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep les
pwm-node verify les/channel_Re180_s1.yaml
pwm-node mine les/channel_Re180_s1.yaml
pwm-node inspect sha256:<cert_hash>
```
