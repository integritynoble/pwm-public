# Principle #186 — Smoothed Particle Hydrodynamics (SPH)

**Domain:** Fluid Dynamics | **Carrier:** N/A (PDE-based) | **Difficulty:** Standard (δ=3)
**DAG:** [K.kernel.wendland] --> [N.bilinear.pair] --> [∂.time.symplectic] |  **Reward:** 3× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          K.kernel.wendland→N.bilinear.pair→∂.time.symplectic      SPH         dam-break-2D      meshfree
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  SMOOTHED PARTICLE HYDRO   P = (E,G,W,C)   Principle #186       │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ dρᵢ/dt = Σⱼ mⱼ(uᵢ−uⱼ)·∇W(rᵢ−rⱼ,h)                 │
│        │ duᵢ/dt = −Σⱼ mⱼ(pᵢ/ρᵢ²+pⱼ/ρⱼ²)∇W + viscous + g    │
│        │ EOS: p = c₀²(ρ−ρ₀) (weakly compressible)              │
│        │ W = kernel (cubic spline, Wendland C2)                 │
│        │ Forward: particle IC → evolve (x,u,ρ,p) for all i    │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [K.kernel.wendland] --> [N.bilinear.pair] --> [∂.time.symplectic]│
│        │ kernel-interp  neighbor-pair-sum  symplectic-time                │
│        │ V={K.kernel.wendland,N.bilinear.pair,∂.time.symplectic}  L_DAG=3.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (particle consistency with sufficient N)│
│        │ Uniqueness: YES for weakly compressible formulation    │
│        │ Stability: CFL on c₀; tensile instability needs fix   │
│        │ Mismatch: smoothing length h, particle disorder       │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = free-surface position error, pressure L2 error     │
│        │ q = 1.0 (standard SPH), 2.0 (corrected SPH)         │
│        │ T = {front_position_err, pressure_error, energy_cons} │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | SPH discretization consistent; kernel normalization verified | PASS |
| S2 | Weakly compressible EOS stable for Ma < 0.1 | PASS |
| S3 | Leapfrog/Verlet time integration converges with particle refinement | PASS |
| S4 | Free-surface position error bounded by particle spacing | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# sph/dam_break_2d_s1.yaml
principle_ref: sha256:<p186_hash>
omega:
  particles: 40000
  domain: [4, 3]   # m
  time: [0, 3.0]
E:
  forward: "WCSPH with Wendland C2 kernel"
  c0: 20.0   # speed of sound
  rho0: 1000
B:
  walls: {type: dummy_particles}
  free_surface: {detected: divergence_based}
I:
  scenario: dam_break_2D
  water_column: {width: 1.0, height: 2.0}
  particle_spacing: [0.02, 0.01, 0.005]
O: [front_position_error, pressure_at_wall, energy_conservation]
epsilon:
  front_error_max: 5.0e-2
  pressure_error_max: 1.0e-1
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 40k particles at dx=0.01; kernel support ~3dx | PASS |
| S2 | Ma = U_max/c₀ < 0.1 satisfied | PASS |
| S3 | Verlet + shifting converges; particle disorder controlled | PASS |
| S4 | Front position < 5% error vs experiment (Martin & Moyce 1952) | PASS |

**Layer 2 reward:** 105 PWM

---

## Layer 3 — spec → Benchmark

```yaml
# sph/benchmark_dam_break.yaml
spec_ref: sha256:<spec186_hash>
principle_ref: sha256:<p186_hash>
dataset:
  name: Martin_Moyce_dam_break
  reference: "Martin & Moyce (1952) experimental data"
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: DualSPHysics (standard)
    params: {N: 40000, kernel: Wendland}
    results: {front_error: 4.2e-2, pressure_err: 8.5e-2}
  - solver: SPHinXsys (δ-SPH)
    params: {N: 40000}
    results: {front_error: 2.8e-2, pressure_err: 5.1e-2}
  - solver: ISPH (pressure Poisson)
    params: {N: 40000}
    results: {front_error: 2.1e-2, pressure_err: 3.2e-2}
quality_scoring:
  - {min_front_err: 1.5e-2, Q: 1.00}
  - {min_front_err: 3.0e-2, Q: 0.90}
  - {min_front_err: 5.0e-2, Q: 0.80}
  - {min_front_err: 8.0e-2, Q: 0.75}
```

**Baseline solver:** δ-SPH — front error 2.8×10⁻²
**Layer 3 reward:** 60 PWM

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | Front Error | Pressure Err | Runtime | Q |
|--------|------------|-------------|---------|---|
| WCSPH (standard) | 4.2e-2 | 8.5e-2 | 60 s | 0.80 |
| δ-SPH | 2.8e-2 | 5.1e-2 | 80 s | 0.90 |
| ISPH | 2.1e-2 | 3.2e-2 | 120 s | 0.90 |
| δ-SPH (fine, 160k) | 1.2e-2 | 2.1e-2 | 400 s | 1.00 |

### Reward Calculation

```
R = 100 × 1.0 × 3 × 1.0 × Q
Best case (δ-SPH fine): 300 × 1.00 = 300 PWM
Floor:                  300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p186_hash>",
  "h_s": "sha256:<spec186_hash>",
  "h_b": "sha256:<bench186_hash>",
  "r": {"residual_norm": 1.2e-2, "error_bound": 5.0e-2, "ratio": 0.24},
  "c": {"fitted_rate": 1.05, "theoretical_rate": 1.0, "K": 3},
  "Q": 1.00,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep sph
pwm-node verify sph/dam_break_2d_s1.yaml
pwm-node mine sph/dam_break_2d_s1.yaml
pwm-node inspect sha256:<cert_hash>
```
