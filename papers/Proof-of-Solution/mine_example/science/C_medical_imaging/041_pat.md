# Principle #41 — Photoacoustic Tomography (PAT)

**Domain:** Medical Imaging | **Carrier:** Acoustic | **Difficulty:** Standard (δ=3)
**DAG:** [G.pulse.laser] --> [K.green.acoustic] --> [S.array]

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          G.pulse.laser --> K.green.acoustic --> S.array        PAT-recon    VesselPhantom-20  BackProj
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  PHOTOACOUSTIC TOMOGRAPHY   P = (E, G, W, C)   Principle #41    │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ (1/c²)∂²p/∂t² − ∇²p = 0; p(r,0) = Γ·μ_a·Φ(r)        │
│        │ y_d(t) = p(r_d, t) + n(t)                              │
│        │ Inverse: recover initial pressure p₀ from boundary     │
│        │ time-series measured by ultrasonic transducer array     │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [G.pulse.laser] ──→ [K.green.acoustic] ──→ [S.array]  │
│        │  Absorb   Propagate  Detect                            │
│        │ V={G.pulse.laser,K.green.acoustic,S.array}  A={G.pulse.laser→K.green.acoustic, K.green.acoustic→S.array}   L_DAG=1.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (wave eqn has unique soln for p₀)       │
│        │ Uniqueness: YES for full-view (closed surface)         │
│        │ Stability: κ ≈ 10 (full-view), κ ≈ 60 (limited-view)  │
│        │ Mismatch: Δc (speed-of-sound), BW (transducer), θ_max │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = PSNR (primary), SSIM (secondary)                  │
│        │ q = 2.0 (universal back-projection convergence)       │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Transducer positions and time samples consistent with geometry | PASS |
| S2 | Full-view angular coverage → bounded inverse via back-projection | PASS |
| S3 | Universal back-projection converges; time-reversal converges | PASS |
| S4 | PSNR ≥ 25 dB achievable for vessel phantom at SNR > 30 | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# pat/vessel_s1_ideal.yaml
principle_ref: sha256:<p041_hash>
omega:
  grid: [256, 256]
  n_detectors: 256
  coverage_deg: 360
  speed_of_sound_m_s: 1500
E:
  forward: "(1/c²)∂²p/∂t² − ∇²p = 0; p(r,0) = Γ·μ_a·Φ"
  PSF: "acoustic Green's function, omnidirectional transducer"
I:
  dataset: VesselPhantom_20
  phantoms: 20
  noise: {type: gaussian, SNR_dB: 40}
  scenario: ideal
O: [PSNR, SSIM]
epsilon:
  PSNR_min: 25.0
  SSIM_min: 0.80
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 256 detectors on full circle satisfy spatial Nyquist for 256×256 grid | PASS |
| S2 | κ ≈ 10 within well-posed regime for full-view | PASS |
| S3 | Back-projection converges for Gaussian noise model | PASS |
| S4 | PSNR ≥ 25 dB feasible for SNR=40 dB acquisition | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# pat/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec041_hash>
principle_ref: sha256:<p041_hash>
dataset:
  name: VesselPhantom_20
  phantoms: 20
  size: [256, 256]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: Universal_BackProjection
    params: {filter: ram-lak}
    results: {PSNR: 25.5, SSIM: 0.82}
  - solver: Time_Reversal
    params: {n_iter: 1}
    results: {PSNR: 28.2, SSIM: 0.88}
  - solver: Model_Based_Iterative
    params: {n_iter: 50, reg: TV}
    results: {PSNR: 31.5, SSIM: 0.93}
quality_scoring:
  - {min: 34.0, Q: 1.00}
  - {min: 30.0, Q: 0.90}
  - {min: 26.0, Q: 0.80}
  - {min: 24.0, Q: 0.75}
```

**Baseline solver:** Universal back-projection — PSNR 25.5 dB
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | PSNR (dB) | SSIM | Runtime | Q |
|--------|-----------|------|---------|---|
| Univ. Back-Projection | 25.5 | 0.82 | 1 s | 0.78 |
| Time Reversal | 28.2 | 0.88 | 10 s | 0.85 |
| Model-Based Iterative | 31.5 | 0.93 | 30 s | 0.92 |
| Deep-PAT (learned) | 35.0 | 0.96 | 2 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 3 × 1.0 × Q
Best case (Deep-PAT):  300 × 1.00 = 300 PWM
Floor:                 300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p041_hash>",
  "h_s": "sha256:<spec041_hash>",
  "h_b": "sha256:<bench041_hash>",
  "r": {"residual_norm": 0.028, "error_bound": 0.055, "ratio": 0.51},
  "c": {"fitted_rate": 1.92, "theoretical_rate": 2.0, "K": 3},
  "Q": 0.92,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep pat
pwm-node verify pat/vessel_s1_ideal.yaml
pwm-node mine pat/vessel_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
