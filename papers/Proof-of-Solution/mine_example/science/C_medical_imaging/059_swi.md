# Principle #59 — Susceptibility-Weighted Imaging (SWI)

**Domain:** Medical Imaging | **Carrier:** Spin | **Difficulty:** Standard (δ=3)
**DAG:** [L.mix.coil] --> [F.dft] --> [S.kspace] --> [K.green.em]

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          L.mix.coil --> F.dft --> S.kspace --> K.green.em    SWI/QSM      BrainSWI-20       MEDI
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  SUSCEPTIBILITY-WEIGHTED IMAGING   P = (E, G, W, C)  Princ #59 │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ φ(r) = γ·B₀·∫ d(r')·χ(r') / |r−r'|³ dr' (dipole)    │
│        │ SWI = magnitude × phase_mask^n                        │
│        │ Inverse: recover susceptibility map χ(r) from GRE     │
│        │ phase via dipole inversion (QSM)                       │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [L.mix.coil] ──→ [F.dft] ──→ [S.kspace] ──→ [K.green.em]│
│        │  Excite  Encode  Sample  Transform Detect              │
│        │ V={L.mix.coil,F.dft,S.kspace,K.green.em}  A={L.mix.coil→F.dft, F.dft→S.kspace, S.kspace→K.green.em}   L_DAG=1.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (GRE phase encodes susceptibility)     │
│        │ Uniqueness: CONDITIONAL (dipole kernel has zero cone)  │
│        │ Stability: κ ≈ 20 (COSMOS multi-orient), κ ≈ 80 (QSM) │
│        │ Mismatch: Δ_unwrap (phase), Δ_background (air/tissue) │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = χ RMSE ppb (primary), PSNR (secondary)            │
│        │ q = 1.0 (MEDI/TKD iterative convergence)             │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | TE and voxel size consistent with susceptibility-weighted contrast | PASS |
| S2 | Regularized dipole inversion well-posed with prior constraints | PASS |
| S3 | MEDI / iLSQR iterative QSM solver converges | PASS |
| S4 | χ RMSE ≤ 20 ppb achievable for brain QSM at 3T | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# swi/brain_s1_ideal.yaml
principle_ref: sha256:<p059_hash>
omega:
  grid: [256, 256, 128]
  voxel_mm: [1, 1, 2]
  TE_ms: 25
  field_T: 3.0
  B0_direction: [0, 0, 1]
E:
  forward: "φ = γ·B₀·d ⊛ χ  (dipole convolution)"
  inversion: "MEDI with L1 regularization"
I:
  dataset: BrainSWI_20
  subjects: 20
  noise: {type: gaussian, SNR: 40}
  scenario: ideal
O: [chi_RMSE_ppb, PSNR]
epsilon:
  chi_RMSE_max_ppb: 20.0
  PSNR_min: 30.0
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 1 mm isotropic at TE=25 ms gives sufficient phase contrast at 3T | PASS |
| S2 | κ ≈ 20 with MEDI regularization within well-posed regime | PASS |
| S3 | MEDI converges for L1-regularized dipole inversion | PASS |
| S4 | χ RMSE ≤ 20 ppb feasible at SNR=40 | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# swi/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec059_hash>
principle_ref: sha256:<p059_hash>
dataset:
  name: BrainSWI_20
  subjects: 20
  size: [256, 256, 128]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: TKD
    params: {threshold: 0.1}
    results: {chi_RMSE_ppb: 35, PSNR: 28}
  - solver: iLSQR
    params: {n_iter: 50}
    results: {chi_RMSE_ppb: 22, PSNR: 32}
  - solver: DeepQSM
    params: {pretrained: true}
    results: {chi_RMSE_ppb: 12, PSNR: 38}
quality_scoring:
  - {max_RMSE: 10, Q: 1.00}
  - {max_RMSE: 15, Q: 0.90}
  - {max_RMSE: 22, Q: 0.80}
  - {max_RMSE: 30, Q: 0.75}
```

**Baseline solver:** TKD — χ RMSE 35 ppb
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | χ RMSE (ppb) | PSNR (dB) | Runtime | Q |
|--------|-------------|-----------|---------|---|
| TKD | 35 | 28 | 1 s | 0.75 |
| iLSQR | 22 | 32 | 30 s | 0.80 |
| DeepQSM (learned) | 12 | 38 | 2 s | 0.95 |
| QSMnet+ | 8 | 42 | 3 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 3 × 1.0 × Q
Best case (QSMnet+):  300 × 1.00 = 300 PWM
Floor:                300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p059_hash>",
  "h_s": "sha256:<spec059_hash>",
  "h_b": "sha256:<bench059_hash>",
  "r": {"residual_norm": 0.025, "error_bound": 0.05, "ratio": 0.50},
  "c": {"fitted_rate": 0.95, "theoretical_rate": 1.0, "K": 3},
  "Q": 0.95,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep swi
pwm-node verify swi/brain_s1_ideal.yaml
pwm-node mine swi/brain_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
