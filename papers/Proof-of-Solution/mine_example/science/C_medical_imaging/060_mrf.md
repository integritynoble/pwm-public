# Principle #60 — MR Fingerprinting (MRF)

**Domain:** Medical Imaging | **Carrier:** Spin | **Difficulty:** Hard (δ=4)
**DAG:** [L.mix.coil] --> [F.dft] --> [S.kspace] --> [N.dictionary]

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          L.mix.coil --> F.dft --> S.kspace --> N.dictionary    MRF-quant    BrainMRF-15       DictMatch
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  MR FINGERPRINTING (MRF)   P = (E, G, W, C)   Principle #60   │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ s(t) = f(T₁, T₂, PD, B₀; TR(t), FA(t))  (Bloch sim) │
│        │ y(t) = s(t) + n(t)  (measured signal evolution)       │
│        │ Inverse: match measured temporal fingerprint to        │
│        │ precomputed dictionary to quantify T₁, T₂, PD jointly │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [L.mix.coil] ──→ [F.dft] ──→ [S.kspace] ──→ [N.dictionary]│
│        │  Excite  Encode  Sample  Transform Detect              │
│        │ V={L.mix.coil,F.dft,S.kspace,N.dictionary}  A={L.mix.coil→F.dft, F.dft→S.kspace, S.kspace→N.dictionary}   L_DAG=1.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (Bloch simulation generates unique sigs)│
│        │ Uniqueness: YES (pseudo-random schedule decorrelates   │
│        │   tissue signatures in dictionary)                     │
│        │ Stability: κ ≈ 10 (1000 TRs), κ ≈ 40 (undersampled)   │
│        │ Mismatch: Δ_B₁ (flip angle), Δ_slice_profile, Δ_flow │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = T₁ RMSE ms (primary), T₂ RMSE ms (secondary)     │
│        │ q = 1.0 (dictionary matching convergence)             │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Pseudo-random TR/FA schedule and dictionary size cover tissue range | PASS |
| S2 | Dictionary inner products separate T₁/T₂ combinations | PASS |
| S3 | Template matching / SVD-compressed matching converges | PASS |
| S4 | T₁ RMSE ≤ 30 ms, T₂ RMSE ≤ 5 ms achievable for brain at 3T | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# mrf/brain_s1_ideal.yaml
principle_ref: sha256:<p060_hash>
omega:
  grid: [256, 256]
  n_time_points: 1000
  TR_range_ms: [12, 15]
  FA_range_deg: [5, 90]
  dict_size: 50000
  field_T: 3.0
E:
  forward: "s(t) = Bloch(T₁,T₂,PD; TR(t),FA(t))"
  model: "pseudo-random TR/FA schedule, Bloch dictionary"
I:
  dataset: BrainMRF_15
  subjects: 15
  noise: {type: gaussian, SNR: 25}
  scenario: ideal
O: [T1_RMSE_ms, T2_RMSE_ms]
epsilon:
  T1_RMSE_max_ms: 30.0
  T2_RMSE_max_ms: 5.0
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 1000 time points with 50k dictionary entries cover physiological range | PASS |
| S2 | κ ≈ 10 within well-posed regime for decorrelated fingerprints | PASS |
| S3 | Dictionary matching converges with SVD compression | PASS |
| S4 | T₁ RMSE ≤ 30 ms feasible at SNR=25 | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# mrf/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec060_hash>
principle_ref: sha256:<p060_hash>
dataset:
  name: BrainMRF_15
  subjects: 15
  size: [256, 256]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: DotProduct_Match
    params: {dict_size: 50000}
    results: {T1_RMSE: 28, T2_RMSE: 4.5}
  - solver: SVD_Compressed
    params: {n_components: 200}
    results: {T1_RMSE: 25, T2_RMSE: 4.0}
  - solver: DeepMRF
    params: {pretrained: true}
    results: {T1_RMSE: 15, T2_RMSE: 2.5}
quality_scoring:
  - {max_T1_RMSE: 12, Q: 1.00}
  - {max_T1_RMSE: 18, Q: 0.90}
  - {max_T1_RMSE: 25, Q: 0.80}
  - {max_T1_RMSE: 30, Q: 0.75}
```

**Baseline solver:** Dot-product match — T₁ RMSE 28 ms
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | T₁ RMSE (ms) | T₂ RMSE (ms) | Runtime | Q |
|--------|-------------|---------------|---------|---|
| Dot-Product Match | 28 | 4.5 | 30 s | 0.75 |
| SVD Compressed | 25 | 4.0 | 5 s | 0.80 |
| DeepMRF (learned) | 15 | 2.5 | 1 s | 0.93 |
| DRONE-Net | 10 | 1.8 | 2 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 4 × 1.0 × Q
Best case (DRONE-Net):  400 × 1.00 = 400 PWM
Floor:                  400 × 0.75 = 300 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p060_hash>",
  "h_s": "sha256:<spec060_hash>",
  "h_b": "sha256:<bench060_hash>",
  "r": {"residual_norm": 0.022, "error_bound": 0.05, "ratio": 0.44},
  "c": {"fitted_rate": 0.95, "theoretical_rate": 1.0, "K": 3},
  "Q": 0.93,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 300–400 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep mrf
pwm-node verify mrf/brain_s1_ideal.yaml
pwm-node mine mrf/brain_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
