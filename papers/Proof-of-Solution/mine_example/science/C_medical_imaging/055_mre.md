# Principle #55 — MR Elastography (MRE)

**Domain:** Medical Imaging | **Carrier:** Spin | **Difficulty:** Standard (δ=3)
**DAG:** [G.pulse] --> [K.elastic] --> [L.mix.coil] --> [F.dft] --> [S.kspace]

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          G.pulse --> K.elastic --> L.mix.coil --> F.dft --> S.kspace    MRE-stiff    LiverMRE-20       LFE
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  MR ELASTOGRAPHY (MRE)   P = (E, G, W, C)   Principle #55     │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ ρ∂²u/∂t² = ∇·(μ∇u);  u = displacement field          │
│        │ φ(r) = γ·G_MEG·∫u(r,t)dt  (motion-encoded MR phase)  │
│        │ Inverse: estimate shear modulus μ(r) from MR phase    │
│        │ images encoding externally driven harmonic shear waves │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [G.pulse] ──→ [K.elastic] ──→ [L.mix.coil] ──→ [F.dft] ──→ [S.kspace]│
│        │  Excite  Propagate Encode   Sample   Detect            │
│        │ V={G.pulse,K.elastic,L.mix.coil,F.dft,S.kspace}  A={G.pulse→K.elastic, K.elastic→L.mix.coil, L.mix.coil→F.dft, F.dft→S.kspace}   L_DAG=1.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (shear waves propagate in soft tissue)  │
│        │ Uniqueness: YES (Helmholtz inversion for single freq.) │
│        │ Stability: κ ≈ 10 (liver), κ ≈ 30 (brain, low SNR)    │
│        │ Mismatch: Δ_frequency, Δ_viscosity, Δ_boundary        │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = stiffness RMSE kPa (primary), bias % (secondary)  │
│        │ q = 2.0 (local frequency estimation convergence)      │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | MEG frequency matches external driver; voxel size resolves λ_shear | PASS |
| S2 | Helmholtz inversion well-posed for harmonic steady-state waves | PASS |
| S3 | LFE / direct inversion converges for multi-frequency data | PASS |
| S4 | Stiffness RMSE ≤ 0.5 kPa achievable for liver MRE at 60 Hz | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# mre/liver_s1_ideal.yaml
principle_ref: sha256:<p055_hash>
omega:
  grid: [256, 256, 32]
  voxel_mm: [1.5, 1.5, 5]
  drive_freq_Hz: 60
  n_phase_offsets: 4
  field_T: 1.5
E:
  forward: "φ(r) = γ·G_MEG·∫u(r,t)dt; μ from ∇²u/u"
  model: "Helmholtz inversion, viscoelastic"
I:
  dataset: LiverMRE_20
  subjects: 20
  noise: {type: gaussian, SNR: 15}
  scenario: ideal
O: [stiffness_RMSE_kPa, bias_pct]
epsilon:
  RMSE_max_kPa: 0.5
  bias_max_pct: 8.0
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 1.5 mm in-plane at 60 Hz resolves λ_shear ≈ 30 mm in liver | PASS |
| S2 | κ ≈ 10 within well-posed regime for single-frequency Helmholtz | PASS |
| S3 | LFE converges for Gaussian noise at SNR=15 | PASS |
| S4 | RMSE ≤ 0.5 kPa feasible for healthy liver (μ ≈ 2.5 kPa) | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# mre/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec055_hash>
principle_ref: sha256:<p055_hash>
dataset:
  name: LiverMRE_20
  subjects: 20
  size: [256, 256, 32]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: LFE
    params: {n_scales: 4}
    results: {stiffness_RMSE: 0.48, bias_pct: 7.5}
  - solver: Direct_Inversion
    params: {filter: Butterworth_3rd}
    results: {stiffness_RMSE: 0.35, bias_pct: 5.0}
  - solver: DeepMRE
    params: {pretrained: true}
    results: {stiffness_RMSE: 0.20, bias_pct: 2.5}
quality_scoring:
  - {max_RMSE: 0.15, Q: 1.00}
  - {max_RMSE: 0.25, Q: 0.90}
  - {max_RMSE: 0.40, Q: 0.80}
  - {max_RMSE: 0.50, Q: 0.75}
```

**Baseline solver:** LFE — RMSE 0.48 kPa
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | RMSE (kPa) | Bias (%) | Runtime | Q |
|--------|-----------|----------|---------|---|
| LFE | 0.48 | 7.5 | 2 s | 0.75 |
| Direct Inversion | 0.35 | 5.0 | 5 s | 0.85 |
| DeepMRE (learned) | 0.20 | 2.5 | 1 s | 0.95 |
| Multi-Freq FEM | 0.12 | 1.5 | 30 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 3 × 1.0 × Q
Best case (Multi-Freq FEM):  300 × 1.00 = 300 PWM
Floor:                       300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p055_hash>",
  "h_s": "sha256:<spec055_hash>",
  "h_b": "sha256:<bench055_hash>",
  "r": {"residual_norm": 0.020, "error_bound": 0.045, "ratio": 0.44},
  "c": {"fitted_rate": 1.94, "theoretical_rate": 2.0, "K": 3},
  "Q": 0.95,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep mre
pwm-node verify mre/liver_s1_ideal.yaml
pwm-node mine mre/liver_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
