# Principle #44 — MR Spectroscopy (MRS)

**Domain:** Medical Imaging | **Carrier:** Spin | **Difficulty:** Standard (δ=3)
**DAG:** [L.mix.coil] --> [F.dft] --> [S.spectral]

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          L.mix.coil --> F.dft --> S.spectral      MRS-quant    BrainMRS-20       LCModel
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  MR SPECTROSCOPY (MRS)   P = (E, G, W, C)   Principle #44      │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ S(f) = Σ_k a_k · L(f − f_k, T₂_k) · exp(iφ_k) + n(f)│
│        │ L = Lorentzian/Voigt line shape per metabolite k       │
│        │ Inverse: fit metabolite concentrations {a_k} from      │
│        │ chemical-shift-resolved spectrum                        │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [L.mix.coil] ──→ [F.dft] ──→ [S.spectral]             │
│        │  Excite  Disperse  Fourier  Detect                     │
│        │ V={L.mix.coil,F.dft,S.spectral}  A={L.mix.coil→F.dft, F.dft→S.spectral}   L_DAG=1.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (FID always acquired)                   │
│        │ Uniqueness: YES (distinct chemical shifts separate      │
│        │   metabolites when linewidth < Δf)                     │
│        │ Stability: κ ≈ 8 (well-shimmed), κ ≈ 40 (poor shim)   │
│        │ Mismatch: ΔB₀ (shim), Δ_phase, Δ_baseline             │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = CRLB% (primary), concentration error (secondary)  │
│        │ q = 2.0 (NLLS fitting convergence rate)               │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Spectral bandwidth and points consistent with chemical-shift range | PASS |
| S2 | Basis set spans metabolite space → bounded NLLS inversion | PASS |
| S3 | LCModel / NLLS fitting converges for Lorentzian line shapes | PASS |
| S4 | CRLB ≤ 10% achievable for NAA at SNR > 15 in vivo | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# mrs/brain_s1_ideal.yaml
principle_ref: sha256:<p044_hash>
omega:
  spectral_points: 2048
  bandwidth_Hz: 2000
  field_T: 3.0
  TE_ms: 30
  voxel_mL: 8
E:
  forward: "S(f) = Σ_k a_k·L(f−f_k,T₂_k)·exp(iφ_k) + n"
  basis: "LCModel basis set (20 metabolites)"
I:
  dataset: BrainMRS_20
  spectra: 20
  noise: {type: gaussian, SNR: 20}
  scenario: ideal
O: [CRLB_pct, concentration_error_pct]
epsilon:
  CRLB_max_pct: 10.0
  conc_error_max_pct: 15.0
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 2048 points at 2000 Hz bandwidth resolves 0.01 ppm peaks at 3T | PASS |
| S2 | κ ≈ 8 within well-posed regime for shimmed voxel | PASS |
| S3 | NLLS fitting converges for 20-metabolite basis set | PASS |
| S4 | CRLB ≤ 10% feasible for SNR=20 with 8 mL voxel at 3T | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# mrs/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec044_hash>
principle_ref: sha256:<p044_hash>
dataset:
  name: BrainMRS_20
  spectra: 20
  spectral_points: 2048
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: LCModel
    params: {basis: default}
    results: {mean_CRLB_pct: 6.5, conc_error_pct: 10.2}
  - solver: TARQUIN
    params: {basis: default}
    results: {mean_CRLB_pct: 7.8, conc_error_pct: 12.5}
  - solver: DeepMRS
    params: {pretrained: true}
    results: {mean_CRLB_pct: 4.2, conc_error_pct: 7.0}
quality_scoring:
  - {max_CRLB: 4.0, Q: 1.00}
  - {max_CRLB: 6.0, Q: 0.90}
  - {max_CRLB: 8.0, Q: 0.80}
  - {max_CRLB: 10.0, Q: 0.75}
```

**Baseline solver:** LCModel — CRLB 6.5%
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | CRLB (%) | Conc. Error (%) | Runtime | Q |
|--------|----------|-----------------|---------|---|
| LCModel | 6.5 | 10.2 | 2 s | 0.85 |
| TARQUIN | 7.8 | 12.5 | 1 s | 0.80 |
| DeepMRS (learned) | 4.2 | 7.0 | 0.5 s | 0.95 |
| Bayesian-MRS | 3.5 | 5.5 | 30 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 3 × 1.0 × Q
Best case (Bayesian-MRS):  300 × 1.00 = 300 PWM
Floor:                     300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p044_hash>",
  "h_s": "sha256:<spec044_hash>",
  "h_b": "sha256:<bench044_hash>",
  "r": {"residual_norm": 0.022, "error_bound": 0.05, "ratio": 0.44},
  "c": {"fitted_rate": 1.95, "theoretical_rate": 2.0, "K": 3},
  "Q": 0.95,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep mrs
pwm-node verify mrs/brain_s1_ideal.yaml
pwm-node mine mrs/brain_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
