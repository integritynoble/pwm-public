# Principle #42 — Optical Coherence Tomography (OCT)

**Domain:** Medical Imaging | **Carrier:** Photon | **Difficulty:** Standard (δ=3)
**DAG:** [G.broadband] --> [K.green.coherent] --> [∫.spectral]

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          G.broadband --> K.green.coherent --> ∫.spectral      OCT-recon    RetinaOCT-20      iFFT
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  OPTICAL COHERENCE TOMOGRAPHY   P = (E, G, W, C)  Principle #42 │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ I(k) = |E_r|² + |E_s|² + 2Re{E_r* E_s}               │
│        │ A(z) = FT⁻¹{ S(k)·r(k) } + n(z)                      │
│        │ Inverse: recover depth reflectivity r(z) from spectral │
│        │ interferogram via inverse Fourier transform             │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [G.broadband] ──→ [K.green.coherent] ──→ [∫.spectral] │
│        │  Propagate Interfere Fourier  Detect                   │
│        │ V={G.broadband,K.green.coherent,∫.spectral}  A={G.broadband→K.green.coherent, K.green.coherent→∫.spectral}   L_DAG=1.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (interferogram always measurable)       │
│        │ Uniqueness: YES (complex conjugate resolved by FD-OCT) │
│        │ Stability: κ ≈ 5 (well-sampled k), κ ≈ 30 (sparse)    │
│        │ Mismatch: Δk (dispersion), δ_motion (axial bulk)      │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = SNR_dB (primary), axial resolution μm (secondary) │
│        │ q = 2.0 (Fourier inversion, spectral domain)          │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Spectral sampling satisfies Nyquist for max imaging depth | PASS |
| S2 | Dispersion-compensated interferogram → bounded FT inversion | PASS |
| S3 | iFFT converges exactly; iterative sparsity methods converge | PASS |
| S4 | SNR ≥ 95 dB achievable for retinal imaging at 50 kHz A-scan | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# oct/retina_s1_ideal.yaml
principle_ref: sha256:<p042_hash>
omega:
  axial_pixels: 1024
  lateral_pixels: 512
  center_wavelength_nm: 840
  bandwidth_nm: 50
  axial_res_um: 6
E:
  forward: "A(z) = FT⁻¹{ S(k)·r(k) } + n"
  PSF: "axial = coherence length, lateral = beam waist"
I:
  dataset: RetinaOCT_20
  volumes: 20
  noise: {type: gaussian, SNR_dB: 35}
  scenario: ideal
O: [SNR_dB, PSNR, SSIM]
epsilon:
  SNR_min_dB: 90.0
  PSNR_min: 28.0
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 1024 spectral samples for 3 mm depth at λ=840 nm satisfies Nyquist | PASS |
| S2 | κ ≈ 5 within well-posed regime for dispersion-free case | PASS |
| S3 | iFFT is exact; iterative denoising converges | PASS |
| S4 | SNR ≥ 90 dB feasible at 35 dB raw SNR with averaging | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# oct/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec042_hash>
principle_ref: sha256:<p042_hash>
dataset:
  name: RetinaOCT_20
  volumes: 20
  size: [1024, 512]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: Standard_iFFT
    params: {window: hann}
    results: {PSNR: 28.5, SSIM: 0.84}
  - solver: Dispersion_Compensated_iFFT
    params: {poly_order: 3}
    results: {PSNR: 31.0, SSIM: 0.89}
  - solver: Sparse_OCT_CS
    params: {n_iter: 100, reg: L1}
    results: {PSNR: 34.2, SSIM: 0.93}
quality_scoring:
  - {min: 36.0, Q: 1.00}
  - {min: 33.0, Q: 0.90}
  - {min: 30.0, Q: 0.80}
  - {min: 28.0, Q: 0.75}
```

**Baseline solver:** Standard iFFT — PSNR 28.5 dB
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | PSNR (dB) | SSIM | Runtime | Q |
|--------|-----------|------|---------|---|
| Standard iFFT | 28.5 | 0.84 | 0.05 s | 0.75 |
| Disp.-Compensated iFFT | 31.0 | 0.89 | 0.1 s | 0.83 |
| Sparse OCT (CS) | 34.2 | 0.93 | 5 s | 0.95 |
| Deep-OCT (learned) | 36.5 | 0.96 | 0.3 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 3 × 1.0 × Q
Best case (Deep-OCT):  300 × 1.00 = 300 PWM
Floor:                 300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p042_hash>",
  "h_s": "sha256:<spec042_hash>",
  "h_b": "sha256:<bench042_hash>",
  "r": {"residual_norm": 0.012, "error_bound": 0.03, "ratio": 0.40},
  "c": {"fitted_rate": 1.98, "theoretical_rate": 2.0, "K": 3},
  "Q": 0.95,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep oct
pwm-node verify oct/retina_s1_ideal.yaml
pwm-node mine oct/retina_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
