# Principle #72 — Coherent Diffractive Imaging (CDI) / Phase Retrieval

**Domain:** Coherent Imaging | **Carrier:** Photon / X-ray | **Difficulty:** Research (δ=5)
**DAG:** K.green.fraunhofer --> N.pointwise.abs2 | **Reward:** 5× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          K.green.fraunhofer-->N.pointwise.abs2    CDI         CDISim-20          HIO/ER
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  COHERENT DIFFRACTIVE IMAGING   P = (E, G, W, C)   #72         │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ I(q) = |F{ρ(r)}|²                                     │
│        │ ρ = complex object density; I = far-field intensity    │
│        │ Phase lost at detector; oversampling ratio σ ≥ 2       │
│        │ Inverse: recover ρ(r) from |F{ρ}|² with support constr│
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [K.green.fraunhofer] --> [N.pointwise.abs2]              │
│        │  FarField  ModSq                                       │
│        │ V={K.green.fraunhofer, N.pointwise.abs2}  A={K.green.fraunhofer-->N.pointwise.abs2}   L_DAG=1.0│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (oversampled diffraction pattern)       │
│        │ Uniqueness: YES for σ ≥ 2 with tight support (2D/3D)  │
│        │ Stability: κ ≈ 10 (tight support), κ ≈ 100 (loose)    │
│        │ Mismatch: partial coherence, support estimate errors   │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = R_factor (primary), phase RMSE (secondary)         │
│        │ q = 1.0 (HIO sub-linear), 2.0 (ER local convergence) │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Oversampling ratio σ ≥ 2 in each dimension; detector pixels cover diffraction extent | PASS |
| S2 | Tight support constraint + oversampling ensures unique phase retrieval | PASS |
| S3 | HIO with β=0.9 converges within 1000 iterations for isolated objects | PASS |
| S4 | R_factor ≤ 0.05 achievable for high-SNR data with tight support | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# cdi/cdisim_s1.yaml
principle_ref: sha256:<p072_hash>
omega:
  grid: [512, 512]
  pixel_um: 0.05
  wavelength_nm: 0.15
  oversampling: 4
  support_radius_px: 64
E:
  forward: "I(q) = |F{rho(r)}|^2"
  constraint: "tight_support + positivity"
I:
  dataset: CDISim_20
  patterns: 20
  noise: {type: poisson, peak: 10000}
  scenario: ideal
O: [R_factor, phase_RMSE_rad]
epsilon:
  R_factor_max: 0.05
  phase_RMSE_max: 0.10
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | 4× oversampling on 512×512 grid with 64 px support radius | PASS |
| S2 | κ ≈ 10 with tight support at 4× oversampling | PASS |
| S3 | HIO converges within 500 iterations at peak=10000 photons | PASS |
| S4 | R_factor ≤ 0.05 feasible for Poisson noise at peak 10000 | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# cdi/benchmark_s1.yaml
spec_ref: sha256:<spec072_hash>
principle_ref: sha256:<p072_hash>
dataset:
  name: CDISim_20
  objects: 20
  size: [512, 512]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: Error-Reduction
    params: {n_iter: 1000}
    results: {R_factor: 0.08, phase_RMSE: 0.15}
  - solver: HIO
    params: {n_iter: 500, beta: 0.9}
    results: {R_factor: 0.04, phase_RMSE: 0.08}
  - solver: RAAR
    params: {n_iter: 500, beta: 0.85}
    results: {R_factor: 0.03, phase_RMSE: 0.06}
quality_scoring:
  - {max_R: 0.03, Q: 1.00}
  - {max_R: 0.05, Q: 0.90}
  - {max_R: 0.08, Q: 0.80}
  - {max_R: 0.12, Q: 0.75}
```

**Baseline solver:** HIO — R_factor 0.04
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | R_factor | Phase RMSE | Runtime | Q |
|--------|----------|------------|---------|---|
| Error-Reduction | 0.08 | 0.15 | 30 s | 0.80 |
| HIO | 0.04 | 0.08 | 15 s | 0.90 |
| RAAR | 0.03 | 0.06 | 18 s | 1.00 |
| OSS (DL-init) | 0.025 | 0.05 | 5 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 5 × 1.0 × Q
Best case (RAAR/OSS):  500 × 1.00 = 500 PWM
Floor:                 500 × 0.75 = 375 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p072_hash>",
  "h_s": "sha256:<spec072_hash>",
  "h_b": "sha256:<bench072_hash>",
  "r": {"residual_norm": 0.025, "error_bound": 0.05, "ratio": 0.50},
  "c": {"fitted_rate": 0.95, "theoretical_rate": 1.0, "K": 4},
  "Q": 1.00,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 375–500 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep cdi
pwm-node verify cdi/cdisim_s1.yaml
pwm-node mine cdi/cdisim_s1.yaml
pwm-node inspect sha256:<cert_hash>
```
