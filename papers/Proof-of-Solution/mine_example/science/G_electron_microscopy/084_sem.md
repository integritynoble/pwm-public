# Principle #84 — Scanning Electron Microscopy (SEM)

**Domain:** Electron Microscopy | **Carrier:** Electron | **Difficulty:** Standard (δ=3)
**DAG:** G.beam --> K.scatter.electron --> S.raster | **Reward:** 3× base

---

## ASCII Pipeline

```
seed ──→ Principle ──→ spec.md ──→ Benchmark ──→ Solution
 │         (E,G,W,C)     (YAML)      (data+baselines)  (solver)
 │          G.beam-->K.scatter.electron-->S.raster    SEM-img     SEM-10k           Denoise/SR
 L1 seeds   L1 out       L2 out       L3 out            L4 out
 200 PWM    immutable    150 PWM      100 PWM           R×δ×Q PWM
```

---

## Layer 1 — Seeds → Principle

```
┌──────────────────────────────────────────────────────────────────┐
│  SEM   P = (E, G, W, C)   Principle #84                        │
├────────┬─────────────────────────────────────────────────────────┤
│   E    │ y(r) = η·[Probe(r) ⊛ δ(r,Z)] + n(r)                  │
│        │ Probe = focused e-beam spot; δ = SE/BSE yield map      │
│        │ η = detector efficiency; n = shot + detector noise      │
│        │ Inverse: recover high-res surface Z from noisy scan y   │
├────────┼─────────────────────────────────────────────────────────┤
│   G    │ [G.beam] --> [K.scatter.electron] --> [S.raster]         │
│        │  E-Beam  Scatter  RasterScan                            │
│        │ V={G.beam, K.scatter.electron, S.raster}  A={G.beam-->K.scatter.electron, K.scatter.electron-->S.raster}   L_DAG=2.5│
├────────┼─────────────────────────────────────────────────────────┤
│   W    │ Existence: YES (SE yield is non-zero for all Z)        │
│        │ Uniqueness: YES for surface topography (monotonic SE)   │
│        │ Stability: κ ≈ 20 (high SNR), κ ≈ 120 (low dose)      │
│        │ Mismatch: beam astigmatism, drift, charging artifacts   │
├────────┼─────────────────────────────────────────────────────────┤
│   C    │ e = PSNR (primary), SSIM (secondary)                   │
│        │ q = 2.0 (BM3D-type convergence)                       │
│        │ T = {residual_norm, fitted_rate, K_resolutions}        │
└────────┴─────────────────────────────────────────────────────────┘
```

### S1-S4 Gate Checks (Layer 1)

| Gate | Check | Result |
|------|-------|--------|
| S1 | Probe diameter matches pixel pitch; detector solid angle consistent | PASS |
| S2 | SE yield function monotonic → bounded inverse | PASS |
| S3 | BM3D / DnCNN converge for Poisson-Gaussian noise model | PASS |
| S4 | PSNR ≥ 28 dB achievable at standard dose (10 pA, 10 μs dwell) | PASS |

---

## Layer 2 — Principle → spec.md

```yaml
# sem/sem10k_s1_ideal.yaml
principle_ref: sha256:<p084_hash>
omega:
  grid: [1024, 1024]
  pixel_nm: 10
  beam_kV: 5
  working_distance_mm: 8
E:
  forward: "y = η·(Probe ⊛ SE_yield(Z)) + n"
  probe: "Gaussian, FWHM=5 nm at 5 kV"
I:
  dataset: SEM_10k
  images: 50
  noise: {type: poisson_gaussian, dose: 100_pA, dwell_us: 10}
  scenario: ideal
O: [PSNR, SSIM]
epsilon:
  PSNR_min: 28.0
  SSIM_min: 0.80
```

### S1-S4 Table (Layer 2)

| Gate | Check on spec | Result |
|------|---------------|--------|
| S1 | Grid 1024×1024 at 10 nm satisfies Nyquist for 5 nm probe | PASS |
| S2 | κ ≈ 20 within well-posed regime at stated dose | PASS |
| S3 | BM3D converges for Poisson-Gaussian model at these SNR levels | PASS |
| S4 | PSNR ≥ 28 dB feasible at 100 pA / 10 μs dwell | PASS |

**Layer 2 reward:** 150 × φ(t) × 0.70 = 105 PWM (designer) + upstream 15% → L1

---

## Layer 3 — spec → Benchmark

```yaml
# sem/benchmark_s1_ideal.yaml
spec_ref: sha256:<spec084_hash>
principle_ref: sha256:<p084_hash>
dataset:
  name: SEM_10k
  images: 50
  size: [1024, 1024]
  data_hash: sha256:<dataset_hash>
baselines:
  - solver: BM3D
    params: {sigma_est: auto}
    results: {PSNR: 29.5, SSIM: 0.841}
  - solver: DnCNN
    params: {pretrained: SEM}
    results: {PSNR: 31.8, SSIM: 0.892}
  - solver: Noise2Void
    params: {blind: true}
    results: {PSNR: 30.7, SSIM: 0.867}
quality_scoring:
  - {min: 33.0, Q: 1.00}
  - {min: 31.0, Q: 0.90}
  - {min: 29.0, Q: 0.80}
  - {min: 27.0, Q: 0.75}
```

**Baseline solver:** BM3D — PSNR 29.5 dB
**Layer 3 reward:** 100 × φ(t) × 0.60 = 60 PWM (builder) + upstream

---

## Layer 4 — Benchmark → Solution

### Solver Comparison

| Solver | PSNR (dB) | SSIM | Runtime | Q |
|--------|-----------|------|---------|---|
| BM3D | 29.5 | 0.841 | 1.5 s | 0.80 |
| Noise2Void | 30.7 | 0.867 | 2.0 s | 0.85 |
| DnCNN | 31.8 | 0.892 | 0.8 s | 0.92 |
| Restormer | 33.4 | 0.921 | 1.5 s | 1.00 |

### Reward Calculation

```
R = R_base × φ(t) × δ × ν_c × Q
  = 100 × 1.0 × 3 × 1.0 × Q
Best case (Restormer): 300 × 1.00 = 300 PWM
Floor:                 300 × 0.75 = 225 PWM
```

### Certificate Snippet

```json
{
  "h_p": "sha256:<p084_hash>",
  "h_s": "sha256:<spec084_hash>",
  "h_b": "sha256:<bench084_hash>",
  "r": {"residual_norm": 0.009, "error_bound": 0.025, "ratio": 0.36},
  "c": {"fitted_rate": 1.93, "theoretical_rate": 2.0, "K": 3},
  "Q": 0.92,
  "gate_verdicts": {"S1":"pass","S2":"pass","S3":"pass","S4":"pass"}
}
```

---

## Reward Summary

| Layer | Seed Reward | Ongoing Royalties |
|-------|-------------|-------------------|
| L1 Principle | 200 PWM | 5% of L4 mints |
| L2 spec.md | 105 PWM | 10% of L4 mints |
| L3 Benchmark | 60 PWM | 15% of L4 mints |
| L4 Solution | — | 225–300 PWM per solve |

---

## Quick-Start

```bash
pwm-node benchmarks | grep sem
pwm-node verify sem/sem10k_s1_ideal.yaml
pwm-node mine sem/sem10k_s1_ideal.yaml
pwm-node inspect sha256:<cert_hash>
```
