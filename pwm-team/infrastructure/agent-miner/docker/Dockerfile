# PWM solver sandbox environment
# TODO: implement per agent-miner/CLAUDE.md
FROM python:3.11-slim
# No network access; /input and /output mount points only
# GPU support via nvidia-docker
WORKDIR /solver
COPY . .
